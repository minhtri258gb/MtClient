{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build SCSS",
			"type": "shell",
			"command": "sass",
			"args": [
				"${file}",
				"${fileDirname}/${fileBasenameNoExtension}.css",
			]
		}
	]
}