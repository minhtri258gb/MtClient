var mtMath = {
	h_debug: false,
	
	init: function() {

		// Log
		if (this.h_debug) {
			console.groupCollapsed("[math] init");
			console.log("h_debug:", this.h_debug);
			console.groupEnd();
		}
	},
};